# Claude Code / Claude.ai skill.
# Emits YAML frontmatter (name + description) followed by the document body —
# the format the Skill loader expects at ~/.claude/skills/<name>/SKILL.md.
output = "claude/SKILL.md"
header = "yaml"
